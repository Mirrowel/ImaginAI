// src/config.ts

// API key and AI client are handled by the backend.
// This file is now largely a placeholder but can be used for other frontend-specific configurations.

export const a = null; // Placeholder to ensure module is not empty
