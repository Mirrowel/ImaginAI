from django.apps import AppConfig


class StoryEngineConfig(AppConfig):
    """Django app config for generation orchestration services."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "story_engine"
